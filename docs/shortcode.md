# Shortcode

## `[tourdaten]`

Renders tour dates for the active season on any page or post.

```
[tourdaten]
```

The shortcode calls the same data path as the REST API (`tour_api_get_tour_data`) so output stays consistent with `/wp-json/tour/v1/tour`.
